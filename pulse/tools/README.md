Ed25519 `keygen`/`sign` tooling for self-update release signing lands here,
ported from Beacon RMM's `agent/tools/{keygen,sign}`. Not implemented yet —
self-update is deferred until after the first vertical slice.
